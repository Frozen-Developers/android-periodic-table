build:
    script:
        - gradle build
        # Prevent build artifacts from being removed before deployment
        - cp -r PeriodicTable/build/outputs/ ../artifacts/
    type: build

deploy:
    script:
        - publish-artifacts $DEPLOY_DESTINATION android-periodic-table ../artifacts/
        - rm -rf ../artifacts/
    type: deploy
